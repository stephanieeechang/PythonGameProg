10/17/2016

Write functions for WordGenerator program
readTxt(): reads the sample text file and returns a list (use split function)

nextWord(): find the words after the given word; input sample text, return a list with all following words and a list with the occurrences of the next words

probability(): calculate the probability that a word could be the next word 

Watched a video explaining how Jansen’s (disgusting) machine works